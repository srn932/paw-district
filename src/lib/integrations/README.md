# Integration adapters

Add server-only adapters here for email, CRM, database, WhatsApp and webhook delivery. Keep credentials in server environment variables and call adapters from a route handler or Server Action, never from a client component.
